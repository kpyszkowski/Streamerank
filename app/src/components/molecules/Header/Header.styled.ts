import { BaseContainer } from '@/components'
import tw from 'twin.macro'

export const StyledContainer = tw(
  BaseContainer,
)`flex items-center justify-between fixed top-0 w-full bg-gradient-to-b from-black/75 z-10`
